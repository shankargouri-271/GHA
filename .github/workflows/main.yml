name: Build and Test Maven Project

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  call-reusable:
    uses: ./.github/workflows/reusable-build.yaml
    with:
      project-path: "GHA"
